package com.engfred.musicplayer.core.domain.model

/**
 * Defines the types of automatic playlists available in the application.
 */
enum class AutomaticPlaylistType {
    RECENTLY_ADDED,
    TOP_SONGS
}